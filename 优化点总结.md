# 文件结构可视化工具 优化点总结

> 本文档按时间顺序整理从 M1 最小闭环到 M2 体验完善及后续测试优化过程中，已经沟通并落地解决的优化点。

## OPT-01 M1 最小闭环

【优化点ID】：OPT-01
【优化点名称】：M1 最小闭环搭建
【优化点介绍】：处于项目开发启动阶段。主要工作是完成可运行桌面程序的最小闭环，包括项目骨架、目录扫描、树形画布、托盘、全局快捷键与单实例。
【优化所作工作】：
- 安装 Rust 工具链、MSVC Build Tools、npm 依赖，并搭建 Tauri 2 + React + TypeScript 项目骨架。
- 实现 Rust 迭代式递归扫描、totalSize 自底向上汇总、错误目录标记、事件分块推送。
- 实现 React Flow 树形画布、节点卡片、扫描进度、拖入文件夹与文件夹选择器。
- 实现托盘菜单、关闭隐藏到托盘、单实例、全局快捷键降级提示。
【优化结果】：生成可运行的 release EXE；项目根目录 100,369 个节点可完整扫描；关闭窗口后进程保持托盘常驻，为后续所有优化提供基础。

## OPT-02 懒加载与默认展开三级

【优化点ID】：OPT-02
【优化点名称】：懒加载与默认展开三级
【优化点介绍】：处于目录展示阶段。解决拖入大目录时一次性加载全部节点导致的渲染卡顿问题。
【优化所作工作】：
- 后端扫描全量目录用于 totalSize 汇总，但只下发前 3 层节点。
- 新增 `expand_nodes` 命令，展开节点时按需返回直接子层。
- 前端新增展开/收起、收起全部、展开一级、默认层级等层级控制。
【优化结果】：拖入项目根目录后默认只渲染 183 个前三层卡片，完整统计为 100,369 个节点；展开时才加载下一层，避免百万级节点一次性进入前端。

## OPT-03 搜索定位

【优化点ID】：OPT-03
【优化点名称】：搜索定位
【优化点介绍】：处于节点检索阶段。支持在当前已加载节点中按名称搜索并定位到画布中心。
【优化所作工作】：
- 顶部增加搜索输入框与结果下拉列表。
- 搜索结果按深度和大小排序，点击后高亮节点并平滑居中。
- 支持 Enter 选择第一条结果，清空按钮一键恢复。
【优化结果】：实测输入 `src` 出现 4 条结果，点击后选中并定位到对应卡片，搜索路径不再需要肉眼滚动查找。

## OPT-04 排序、占比与四级连线

【优化点ID】：OPT-04
【优化点名称】：排序、占比与四级连线
【优化点介绍】：处于节点信息展示阶段。用于直观体现节点大小关系与目录占比。
【优化所作工作】：
- 子节点默认按 totalSize 降序排列，支持切换为名称排序。
- 卡片显示占比，支持根目录/父目录两种基准，根节点固定 100%，空目录不除零。
- 连线粗细按子节点占父节点比例分为 4/3/2/1 四级。
【优化结果】：大文件和占比信息一眼可见，连线粗细边界规则明确，实测红/蓝/绿三档进度条与四级粗细均正常。

## OPT-05 文件操作闭环

【优化点ID】：OPT-05
【优化点名称】：文件操作闭环
【优化点介绍】：处于功能完善阶段。将画布从只读展示升级为可执行文件系统操作的工作台。
【优化所作工作】：
- 后端实现打开、打开所在位置、重命名、复制、移动、删除（回收站）、新建命令。
- 前端增加右键菜单、双击打开、F2 重命名、Enter 打开、Delete 删除。
- 操作成功后同步更新画布节点，目录变更后提示重新扫描。
【优化结果】：文件/文件夹操作与画布实时联动；删除默认进回收站；占用、无权限、重名等错误以提示展示且不中断其他操作。

## OPT-06 新建两段式交互

【优化点ID】：OPT-06
【优化点名称】：新建两段式交互
【优化点介绍】：处于新建操作阶段。优化新建文件/文件夹时的类型选择与命名流程。
【优化所作工作】：
- 文件夹卡片增加“新建”按钮，右键菜单增加“新建文件/新建文件夹”。
- 弹窗先选择类型，再填写名称；类型包含文件夹、.doc、.docx、.fpd、.ppt、.pptx、.txt、.md、.xls、.xlsx 及常见压缩包后缀。
- 文件名称自动拼接所选后缀，真实创建文件/文件夹并同步到画布。
【优化结果】：实测选择 `.md` 后输入 `readme` 创建出 `readme.md`，磁盘与画布同步出现，减少手动输入后缀的错误。

## OPT-07 扫描缓存

【优化点ID】：OPT-07
【优化点名称】：扫描缓存
【优化点介绍】：处于重复打开目录阶段。解决二次打开同一目录仍需全量扫描的问题。
【优化所作工作】：
- 扫描结果按目录路径写入用户 AppData 缓存目录。
- 二次打开命中缓存时直接加载，不再扫描文件系统。
- 增加“刷新”强制重扫与“清理缓存”功能。
【优化结果】：项目根目录首次扫描 16.6s，二次打开 0ms，节点数一致；强制刷新 13.8s；缓存清理后缓存目录文件数为 0。

## OPT-08 设置界面、排除规则与深目录测试

【优化点ID】：OPT-08
【优化点名称】：设置界面、排除规则与深目录测试
【优化点介绍】：处于可配置性与稳定性测试阶段。补充用户可配置项和边界场景验证。
【优化所作工作】：
- 新增设置弹窗：默认展开深度、子节点排序、动效强度、排除目录、缓存清理。
- 排除目录规则接入扫描器，被排除目录标记为 filtered 且不继续下钻。
- 新增 200 层深目录专项测试，验证迭代式扫描不爆栈。
【优化结果】：`cargo test` 7 个用例全部通过；排除 `node_modules` 后只显示该节点、不展开内部文件；200 层目录扫描正常。

## OPT-09 进度条卡片

【优化点ID】：OPT-09
【优化点名称】：进度条卡片
【优化点介绍】：处于视觉展示阶段。将卡片占比从纯文字升级为直观背景进度条。
【优化所作工作】：
- 卡片背景增加半透明进度条，宽度对应节点占比。
- 占比 >75% 红色、>50% 蓝色、<=50% 绿色，颜色柔和且不影响文字阅读。
- 切换占比模式或刷新时，进度条从 0 动画到当前占比。
【优化结果】：实测 90% 红色、60% 蓝色、40% 绿色；模式切换后 80ms 约 58px，约 1s 后增长到 225px。

## OPT-10 局部动画

【优化点ID】：OPT-10
【优化点名称】：局部动画
【优化点介绍】：处于展开收起动画阶段。解决展开子结构时全部卡片重复动画的问题。
【优化所作工作】：
- 动画触发改为只作用于实际新增/新出现的节点集合。
- “展开一级”只动画本次新增子树，“收起一层”不触发全量动画。
【优化结果】：展开后上级卡片宽度完全不变，只有 `deep.txt` 从 23.28px 动画到 83.55px；收起一层后上级卡片同样保持不动。

## OPT-11 收起一层与默认层级

【优化点ID】：OPT-11
【优化点名称】：收起一层与默认层级
【优化点介绍】：处于批量层级控制阶段。补齐与“展开一级”相反的批量收拢操作，并让默认层级跟随设置。
【优化所作工作】：
- 新增“收起一层”，与“展开一级”逻辑相反，一次收拢各分支最深层目录。
- 原“默认三级”改名为“默认层级”，按设置中的默认展开深度逐层加载并展开。
【优化结果】：实测展开后 maxDepth 3，点击收起一层回到 2；默认深度设为 5 后点击默认层级，画布展开到对应深度并加载出更深的 `deep.txt`。

## OPT-12 顶部按钮顺序

【优化点ID】：OPT-12
【优化点名称】：顶部按钮顺序
【优化点介绍】：处于界面布局微调阶段。将操作频率相近的按钮按使用逻辑排列。
【优化所作工作】：调整顶部按钮顺序，把“收起一层”放在“展开一级”之前。
【优化结果】：顶部顺序为：收起全部、收起一层、展开一级、默认层级、刷新、选择文件夹、设置、清空。

## OPT-13 镜头稳定与结构过渡动画

【优化点ID】：OPT-13
【优化点名称】：镜头稳定与结构过渡动画
【优化点介绍】：处于结构变化动画阶段。解决展开/收起时镜头突然跳转、卡片和连线瞬移的问题。
【优化所作工作】：
- 移除 React Flow 自动 fit，仅在扫描完成/刷新时主动适配视图。
- 节点位置增加 transform 过渡动画。
- 自定义边组件使用 SMIL 对连线路径 `d` 做插值动画。
- 过渡时长跟随动效设置：标准 260ms、减弱 120ms、关闭 0ms。
【优化结果】：展开前后 viewport 完全不变；`dirB` 位置 96 → 106.98 → 189.28 → 192 平滑移动；连线路径 50ms 时处于 153.5 的中间值，550ms 到达 159。

## OPT-14 根节点固定与顶部对齐布局

【优化点ID】：OPT-14
【优化点名称】：根节点固定与顶部对齐布局
【优化点介绍】：处于布局稳定性阶段。解决子树变化导致根节点和未变化节点整体上下平移的问题。
【优化所作工作】：
- 树形布局从“父节点居中”改为“顶部对齐”。
- 根节点固定在 `(0, 0)`，父节点贴住第一个子节点。
【优化结果】：展开/收起全程根节点 transform 恒为 `(0,0)`，镜头不动；只有受影响的下方节点平滑位移，未变化节点不再重播动画。

## OPT-15 全局快捷键 Z+X+C

【优化点ID】：OPT-15
【优化点名称】：全局快捷键 Z+X+C
【优化点介绍】：处于系统集成阶段。实现无修饰键三键组合的全局唤起/隐藏。
【优化所作工作】：
- 原 `Win+Shift+F` 改为 `Z+X+C` 三键同时按下。
- 无修饰键三键组合无法使用 RegisterHotKey，改用 `device_query` 全局按键状态轮询实现。
【优化结果】：release 版实测按组合键可隐藏窗口，再次按下可唤起，均正常。

## OPT-16 顶部布局单行化

【优化点ID】：OPT-16
【优化点名称】：顶部布局单行化
【优化点介绍】：处于界面布局优化阶段。解决搜索栏过宽导致按钮换行的问题。
【优化所作工作】：
- 搜索栏宽度收窄到 170px。
- 压缩按钮间距、内边距和字号，顶部禁止换行。
【优化结果】：实测搜索栏与全部按钮位于同一行，无横向溢出，顶部高度保持单行。

## OPT-17 右键菜单注册与拖拽打开

【优化点ID】：OPT-17
【优化点名称】：右键菜单注册与拖拽打开
【优化点介绍】：处于 Windows 系统集成阶段。支持文件/文件夹右键“用 FileVisualizer 打开”以及拖拽到 exe 直接打开。
【优化所作工作】：
- 在 HKCU 注册文件与文件夹右键菜单，命令为 `"exe路径" "%1"`。
- 启动参数和单实例转发统一进入自动扫描通道。
- 修复单实例转发时误把 exe 自身路径当作目标的问题，改为跳过 `args[0]`。
- 右键菜单改为设置中的“启用/禁用”可选项，默认不注册，启用后持久化并在启动时刷新注册路径。
【优化结果】：第一次打开与后台运行时换文件打开均正确扫描目标；启用/禁用、重启保持、注册表写入与移除均实测通过。

## OPT-18 右键“打开”功能修复

【优化点ID】：OPT-18
【优化点名称】：右键“打开”功能修复
【优化点介绍】：处于文件操作回归阶段。修复右键菜单“打开”不生效的问题。
【优化所作工作】：
- 新增后端 `open_node` 命令，使用系统默认程序打开文件/文件夹。
- 右键菜单“打开”、双击卡片、Enter 快捷键统一改为调用该 Rust 命令。
- 调用失败时在前端显示错误提示。
【优化结果】：实测对 `test.txt` 右键点击“打开”无报错，并成功拉起系统默认程序；构建与测试均通过。

## OPT-19 视口裁剪与节点复用

【优化点ID】：OPT-19
【优化点名称】：视口裁剪与节点复用
【优化点介绍】：处于 M3 性能优化阶段。解决十万级节点全部进入 React Flow 后渲染与重绘压力过大的问题。
【优化所作工作】：
- 启用 React Flow `onlyRenderVisibleElements`，画布只渲染视口内元素。
- 节点数量超过阈值时，按视口变换裁剪节点与连线，仅向 React Flow 传入可见集合。
- 节点卡片使用 `memo` 复用，未变化的节点不再随父组件重建。
【优化结果】：前端构建通过；大目录缩放和平移时只维护视口附近节点，降低十万级场景下的渲染压力。

## OPT-20 十万级压测

【优化点ID】：OPT-20
【优化点名称】：十万级压测
【优化点介绍】：处于 M3 性能验证阶段。为画布提供可重复的十万节点性能测试入口。
【优化所作工作】：
- 新增后端 `generate_stress_scan` 命令，在内存中生成 100,000 节点合成树。
- 空画布增加“示例文件展示”按钮，载入后可直接缩放、平移和展开验证流畅度。
- 新增 Rust 压测用例，覆盖合成树构建与 totalSize 自底向上汇总。
【优化结果】：`cargo test` 9 个用例全部通过，其中十万节点合成树用例通过；压测入口已可在应用内使用。

## OPT-21 二进制缓存

【优化点ID】：OPT-21
【优化点名称】：二进制缓存
【优化点介绍】：处于 M3 缓存优化阶段。解决 JSON 缓存体积大、读写耗时长的问题。
【优化所作工作】：
- 新增 `.fvc` 紧凑二进制缓存格式，包含版本魔数与长度前缀字符串编码。
- 保存扫描结果时写入 `.fvc`，读取时优先二进制并兼容旧 `.json` 缓存。
- 新增二进制缓存编解码回读单元测试。
【优化结果】：缓存体积显著下降，二次打开仍走零扫描缓存路径；旧缓存可平滑回退读取。

## OPT-22 安装包与正式版

【优化点ID】：OPT-22
【优化点名称】：安装包与正式版
【优化点介绍】：处于 M3 交付阶段。补齐 Windows 安装包和正式版发布说明。
【优化所作工作】：
- 启用 `bundle.active`，配置 NSIS / MSI 目标、图标、WebView2 引导与安装语言。
- 版本号统一为 1.0.0。
- 生成 NSIS 与 MSI 安装包，并补充 `使用说明.md`。
【优化结果】：NSIS 约 2.14 MB，MSI 约 3.15 MB；安装包与 release EXE 均生成成功。

## OPT-23 全量搜索与自动展开定位

【优化点ID】：OPT-23
【优化点名称】：全量搜索与自动展开定位
【优化点介绍】：处于搜索定位增强阶段。解决未展开节点无法被搜索到的问题。
【优化所作工作】：
- 新增后端 `search_nodes` 命令，在全量扫描图中按名称搜索。
- 搜索结果携带从根节点到父节点的祖先路径。
- 选中结果时自动按需加载并展开祖先节点，随后定位选中目标。
【优化结果】：前端构建与 Rust 10 个测试通过；未展开的深层文件也能搜索到并自动展开定位。
