# 文件结构可视化工具 工作交接文档

> 交接日期：2026-08-03  
> 项目根目录：`C:\Users\cxz\Desktop\personal practice\File visualization`  
> 当前进度：M1、M2、M3 已完成并验证，正式版 v1.0.0 已交付。

## 1. 项目概况

本项目是一个轻量级 Windows 桌面工具，用于拖入本地文件夹后递归扫描并可视化目录结构，展示节点大小、占比和层级关系，并支持常用文件系统操作。

技术栈：

- 框架：Tauri 2
- 后端：Rust
- 前端：React 19 + TypeScript + Vite 8
- 画布：React Flow 12
- 状态管理：Zustand 5
- 包管理：pnpm 11

核心文档：

- 需求文档：`需求文档.md`
- 优化点总结：`优化点总结.md`
- 本交接文档：`工作交接文档.md`

## 2. 环境与工具链

当前机器已安装并配置：

- Node.js v24.16.0
- pnpm 11.9.0
- npm 11.13.0
- Rust stable 1.97.1，MSVC target
- Visual Studio Build Tools：`C:\BuildTools`，MSVC 19.44
- WebView2 Runtime 已安装

注意：Rust 工具链安装在项目本地目录，不在系统 PATH 中：

- rustup home：`.toolchain\rustup`
- cargo home：`.toolchain\cargo`

运行 Rust/Cargo 命令前需要设置：

```powershell
$env:RUSTUP_HOME = (Join-Path $PWD ".toolchain\rustup")
$env:CARGO_HOME = (Join-Path $PWD ".toolchain\cargo")
$env:PATH = (Join-Path $env:CARGO_HOME "bin") + ";" + $env:PATH
```

常用命令：

```powershell
# 安装前端依赖（pnpm 11 需要 CI 变量避免 TTY 交互）
$env:CI = "true"; pnpm install --config.confirmModulesPurge=false

# 前端类型检查 + 构建
$env:CI = "true"; pnpm build

# Rust 测试
cargo test

# 编译 release 可执行文件（不打安装包）
$env:CI = "true"; pnpm tauri build --no-bundle
```

Release 可执行文件位置：

```text
src-tauri\target\release\file-visualizer.exe
```

## 3. 当前功能状态

### 已完成的 M1

- Tauri 2 + React + TypeScript 项目骨架
- Rust 递归扫描、totalSize 汇总、错误目录标记
- React Flow 树形画布、节点卡片、扫描进度
- 托盘常驻、关闭隐藏到托盘、单实例
- 全局快捷键：`Z + X + C` 三键同时按下唤起/隐藏

### 已完成的 M2

- 懒加载：后端只下发默认深度内节点，展开时按需返回子层
- 默认展开深度可由设置调整，支持“默认层级”按钮
- 展开/收起、收起全部、收起一层、展开一级
- 搜索定位
- 子节点按大小/名称排序
- 根目录/父目录占比基准切换
- 四级连线粗细
- 进度条卡片（红/蓝/绿分档、局部动画）
- 文件操作：打开、打开所在位置、重命名、复制到、移动到、删除（回收站）、新建
- 新建两段式交互：先选类型，再填名称，自动补后缀
- 扫描缓存：首次落盘、二次秒开、刷新、一键清理
- 设置界面：默认深度、排序、动效、排除规则、缓存、右键菜单
- 右键菜单“用 FileVisualizer 打开”注册/卸载（设置中可选项）
- 拖拽文件/文件夹到 exe 直接打开
- 顶部布局单行化、镜头稳定、根节点固定、节点/连线平滑过渡

### 已完成的 M3

- 视口裁剪与节点复用：React Flow `onlyRenderVisibleElements`、视口外节点/连线裁剪、节点组件 memo 复用
- 十万级压测：空画布“示例文件展示”入口载入 100,000 节点示例树；Rust 压测用例覆盖构建与 totalSize 汇总
- 二进制缓存：扫描缓存由 JSON 改为 `.fvc` 紧凑二进制格式，兼容读取旧 JSON 缓存
- 全量搜索：搜索覆盖全部已扫描节点，选中未展开结果时自动加载并展开祖先路径、定位选中目标
- 安装包：配置 NSIS / MSI 目标并生成 v1.0.0 安装包
- 正式版本：版本号统一为 1.0.0，补充使用说明

### 未实现/待办

- 可选：系统剪贴板 Ctrl+C/Ctrl+V 粘贴
- 可选：多文件夹多根节点并列
- 可选：导出结构图（PNG/SVG/JSON）
- 可选：缓存进一步压缩、代码签名、自动更新

## 4. 架构与关键文件

### 后端

- `src-tauri/src/lib.rs`：Tauri 入口、命令注册、托盘、全局快捷键、右键注册、启动路径、单实例转发
- `src-tauri/src/scan.rs`：扫描器、ScanGraph、totalSize、排序、缓存读写、单元测试
- `src-tauri/Cargo.toml`：依赖清单
- `src-tauri/tauri.conf.json`：窗口、构建、bundle 配置
- `src-tauri/capabilities/default.json`：权限配置

### 前端

- `src/App.tsx`：主界面、事件监听、搜索、设置、右键菜单、新建弹窗
- `src/store.ts`：Zustand 状态、展开状态、动画计数、设置持久化
- `src/layout.ts`：可见节点计算、顶部对齐树形布局
- `src/components/FileNode.tsx`：节点卡片、进度条、重命名、展开/新建按钮
- `src/components/FlowCanvas.tsx`：React Flow 画布、节点/边组装、fitView
- `src/components/SmoothEdge.tsx`：自定义连线，SMIL 路径插值动画
- `src/fileTypes.ts`：新建文件类型清单
- `src/types.ts`、`src/format.ts`、`src/styles.css`

## 5. 关键实现细节

### 扫描与懒加载

- `scan_path` 命令接收 `path`、`refresh`、`defaultDepth`、`excludes`。
- 扫描完成后全量图保存在 `AppState.current_scan`。
- 前端只接收 `depth <= defaultDepth` 的初始节点。
- `expand_nodes` 从当前图返回指定节点的直接子层。
- 缓存文件位于 `%APPDATA%\com.cxz.filevisualizer\cache\<hash>.fvc`，旧 `.json` 缓存仍可回退读取。

### 布局与动画

- 布局使用顶部对齐：根节点固定 `(0,0)`，父节点贴第一个子节点。
- 节点位置使用 CSS transform 过渡。
- 连线使用自定义 `SmoothEdge` + SMIL 动画。
- 进度条动画分两种触发：
  - 全局动画：切换占比模式、刷新/扫描完成
  - 局部动画：展开时只动画实际新增节点集合

### 全局快捷键

- 使用 `device_query` 轮询全局按键状态。
- 组合键为 `Z + X + C`，50ms 轮询一次。
- release 版已验证可用。

### Windows 右键菜单与拖拽打开

- 注册位置：
  - `HKCU\Software\Classes\*\shell\FileVisualizer`
  - `HKCU\Software\Classes\Directory\shell\FileVisualizer`
- 命令格式：`"exe绝对路径" "%1"`
- 设置中“右键菜单”为可选项，默认禁用；启用后持久化，并在启动时刷新注册路径。
- 单实例转发会跳过 `args[0]`，避免把 exe 自身路径当成目标。

## 6. 测试与验证现状

`cargo test` 当前 9 个用例全部通过：

- totalSize 自底向上汇总
- 空目录 totalSize 为 0
- 真实临时目录扫描
- 初始节点深度限制
- 子节点按大小排序
- 排除目录规则
- 200 层深目录不爆栈
- 二进制缓存编解码回读
- 10 万节点合成树构建与 totalSize 汇总

`pnpm build`、`pnpm tauri build --no-bundle` 均通过。

安装包已验证生成：

- NSIS：`FileVisualizer_1.0.0_x64-setup.exe`，约 2.14 MB
- MSI：`FileVisualizer_1.0.0_x64_zh-CN.msi`，约 3.15 MB

已实测的关键指标：

- 默认展开 3 层：项目根目录显示 183 张卡片，全量节点 100,369 个
- 缓存：首次 16.6s，二次打开 0ms，强制刷新 13.8s
- 进度条：红/蓝/绿三档、模式切换和刷新动画正常
- 局部动画：展开一级只动画新增子树，上级卡片不动
- 根节点固定：展开/收起全程 transform 恒为 `(0,0)`
- 连线动画：路径 d 有中间插值
- 全局快捷键：release 版隐藏/唤起正常
- 右键菜单：启用/禁用、重启保持、注册表写入/移除验证通过
- 拖拽/右键二次打开：第二次打开正确扫描新目标，不再误扫 exe
- 右键“打开”：调用系统默认程序成功

## 7. 已知问题与技术债

- 十万级合成树压测已加入，后续可在真实十万级目录上继续采集首屏、缩放和平移基线。
- 缓存已改为 `.fvc` 二进制格式，体积显著下降；尚未增加压缩层。
- 安装包已生成；未配置代码签名，首次运行可能出现 SmartScreen 提示。
- 系统剪贴板 Ctrl+C/Ctrl+V 粘贴未实现，目前只有“复制到...”菜单。
- 多文件夹拖入仍按单根处理，多根并列属于开放问题。
- 全局快捷键采用 50ms 轮询，足够日常使用；如需更低延迟可考虑后续优化。
- 在自动化沙箱环境中，提权上下文与普通上下文看到的 HKCU 不同；验证右键注册需要留意运行上下文。

## 8. M3 完成情况与后续建议

### 已完成

- 视口裁剪与节点复用
- 十万级合成树压测
- 扫描缓存二进制格式
- NSIS / MSI 安装包
- 正式版使用说明

### 后续建议

- 配置代码签名与自动更新
- 实现系统剪贴板粘贴、多根并列、结构图导出
- 在真实十万级目录上采集 30fps、首屏时间与内存占用基线

## 9. 接手检查清单

1. 阅读 `需求文档.md`。
2. 阅读 `优化点总结.md` 与本交接文档。
3. 运行 `pnpm build`、`cargo test` 确认环境可用。
4. 运行 `pnpm tauri build --no-bundle` 确认 release 编译正常。
5. 运行 `src-tauri\target\release\file-visualizer.exe` 验证基础功能。
