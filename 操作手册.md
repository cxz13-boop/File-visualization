# 文件结构可视化工具 操作手册

版本：v1.0.0  
适用系统：Windows 10 / Windows 11 x64  
适合读者：第一次使用这个软件的人

## 1. 这个软件能做什么

文件结构可视化工具可以把一个文件夹里的内容变成一张可以缩放、平移的树形图。每个文件或文件夹都会显示成一张卡片，卡片上有名称、大小、占比和修改时间。文件夹卡片还会显示它里面一共有多少个子项。

扫描结果只保存在本地。你可以用它查看某个目录占了多少空间，也可以直接在画布里打开文件、重命名、移动、删除或新建文件。

## 2. 安装软件

安装包有两个版本，任选一个即可：

- NSIS 安装包：`FileVisualizer_1.0.0_x64-setup.exe`
- MSI 安装包：`FileVisualizer_1.0.0_x64_zh-CN.msi`

双击安装包，按安装向导提示操作。安装过程中如果提示缺少 WebView2 运行时，程序会自动引导下载安装。

安装包目前没有做代码签名，Windows 第一次运行时可能会显示 SmartScreen 提示。如果出现这个提示，选择“仍要运行”即可。

安装完成后，程序会自动启动并常驻系统托盘。托盘里有一个文件夹样式的图标。

## 3. 启动、退出和托盘

程序启动后会显示主窗口。即使你关闭窗口，程序也不会退出，它会隐藏到托盘继续运行。

托盘图标可以这样操作：

- 左键单击托盘图标：显示或隐藏主窗口
- 右键打开托盘菜单：选择“显示 / 隐藏”或“退出”

想真正退出程序时，用托盘菜单里的“退出”。只关窗口的话，程序还在后台。

全局快捷键是 `Z + X + C`，三个键同时按下，可以随时显示或隐藏主窗口。即使窗口在别的程序后面，也能直接调出来。

程序支持单实例运行。重复启动程序时，不会开第二个窗口，只会把已有窗口显示出来。

## 4. 第一次打开时的界面

主界面分成三块：

顶部是操作栏。左边是软件名称，中间是搜索框，右边是一排按钮。

中间是画布。没有加载目录时，画布中央会显示“拖入文件夹”，下方有一个“示例文件展示”按钮。

底部是状态栏。加载目录后，会显示当前路径、节点总数、总大小和扫描耗时。

扫描进行时，顶部操作栏下方会出现一行扫描状态，显示已经扫描了多少项，以及当前正在扫描的路径。

## 5. 加载一个文件夹

有两种方式：

第一种，直接把文件夹从资源管理器拖到画布中央，然后松开鼠标。

第二种，点击顶部“选择文件夹”按钮，在弹出的窗口里选中一个文件夹。

程序会先扫描目录，再显示结果。扫描时间取决于文件夹里的文件数量。同一个目录第二次打开时会使用本地缓存，通常会快很多。

也可以把一个文件或文件夹直接拖到 exe 文件图标上，程序会启动并自动扫描这个目标。

画布为空时，还可以点击“示例文件展示”按钮，载入一份 10 万节点的示例目录。这个示例数据只存在于内存里，不会改动你电脑上的文件，适合用来熟悉画布操作。

## 6. 画布怎么看

目录结构按照树形平铺。根目录固定在最左边，子目录往右延伸。

平移画布：按住画布空白处拖动。

缩放画布：滚动鼠标滚轮。

画布左下角有缩放和适应窗口控件，可以放大、缩小和适应画布。右下角是小地图，方便看出当前看到的是整棵树的哪个位置。

## 7. 节点卡片上的内容

文件夹卡片显示：

- 文件夹图标和名称
- 这个文件夹以及它里面所有内容的累计大小
- 直接子项数量
- 当前节点占根目录或父目录的比例
- 一条背景进度条，表示占比大小

文件卡片显示：

- 文件图标和名称
- 文件自身大小
- 占比
- 最后修改时间

根目录卡片上会有一个“根目录”标记。

如果某个文件夹没有权限读取，卡片上会显示一个错误图标。这类节点不会中断整个扫描，其他内容照常显示。

进度条颜色按占比分档：占比超过 75% 是一种颜色，超过 50% 是另一种颜色，50% 以下是第三种颜色。颜色只用于快速区分大小，具体数值以卡片上的百分比为准。

父节点到子节点的连线也有粗细区别。子节点占父节点的比例越大，连线越粗。

## 8. 展开和收起节点

文件夹卡片右侧有一个展开按钮。

点击展开按钮，会显示这个文件夹的直接子层。子节点默认是收起状态，不会连带着把更深层全部展开。

点击收起按钮，会把这个文件夹以及它下面已经展开的所有层级一起收起。之后再点展开，只显示它自己的直接子层，不会恢复之前展开过的深层状态。

如果文件夹的子节点还没有加载，第一次点击展开时会先从后台加载，再显示。

顶部还有几个批量操作按钮，专门用来处理多层结构。

## 9. 顶部按钮逐个说明

### 根目录 / 父目录

这两个按钮用来切换占比基准。

选择“根目录”时，每个节点的占比都是按整棵树的根目录总大小计算。

选择“父目录”时，每个节点的占比按它自己的父文件夹总大小计算。

根节点无论哪种模式都显示 100%。

### 收起全部

把画布里所有展开的节点全部收起，只留下根目录。已经加载到内存的深层节点不会删除，只是暂时不显示。

### 收起一层

把每个分支里最深的已展开层级收起来。如果整棵树已经收到只剩根目录，再点这个按钮不会有变化。

### 展开一级

把所有当前可见的文件夹都展开一层。已经展开的节点不会重复展开，也不会把更深层一起打开。

### 默认层级

按照设置里的“默认展开深度”重新整理画布。

点击后，程序会先收起全部，再逐层加载并展开到设置里指定的深度，最后自动调整画布视角，让整棵展开后的树都能看到。

如果设置里的默认深度是 3，这个按钮就会把树展开到第 3 层。

### 刷新

重新扫描当前目录，并忽略本地缓存。适合在文件夹内容发生变化后使用。

### 选择文件夹

打开系统文件夹选择窗口，加载一个新的目录。

### 设置

打开设置窗口。设置窗口里的每一项在第 12 节有详细说明。

### 清空

清空当前画布，回到“拖入文件夹”的初始状态。清空不会删除磁盘上的任何文件。

## 10. 搜索框

搜索框在顶部中间。

输入文件名的一部分，程序会在整个已扫描结果里查找，包括还没有展开的深层节点。搜索结果会显示文件或文件夹名称以及完整路径。

点击某一条搜索结果，程序会自动加载并展开它前面的所有祖先文件夹，然后把这条结果移动到画布中间并选中。按回车键可以选中当前第一条结果。

搜索框右侧的叉号可以清空搜索词。

如果还没有加载任何目录，搜索框不会返回结果。

## 11. 鼠标和键盘操作

单击节点：选中节点。

双击节点：用系统默认程序打开这个文件或文件夹。

右键节点：打开右键菜单。

键盘快捷键：

- `Enter`：打开当前选中的节点
- `F2`：重命名当前选中的节点
- `Delete`：删除当前选中的节点，并移入回收站

删除前会弹出确认窗口。删除文件夹时，会把整个文件夹移入回收站。

## 12. 右键菜单

在文件夹卡片上右键，菜单里会出现：

- 新建文件
- 新建文件夹
- 打开
- 打开所在位置
- 重命名
- 复制到...
- 移动到...
- 删除

在文件卡片上右键，菜单里不会出现“新建文件”和“新建文件夹”，其他项目一样。

### 新建文件

先选择文件类型，再输入名称。程序会自动补上后缀。

可选类型包括：

- 文件夹
- `.doc`、`.docx`
- `.fpd`
- `.ppt`、`.pptx`
- `.txt`、`.md`
- `.xls`、`.xlsx`
- `.zip`、`.rar`、`.7z`
- `.tar`、`.gz`、`.tar.gz`、`.bz2`、`.xz`

新建操作会真实创建到磁盘上，同时出现在画布里。

### 新建文件夹

输入文件夹名称后直接创建。

### 打开

调用系统默认程序打开这个文件或文件夹。

### 打开所在位置

在资源管理器里打开目标所在的文件夹，并选中这个文件或文件夹。

### 重命名

在卡片内直接修改名称。改完后按回车确认，按 `Escape` 取消。

如果新名称和同一目录下已有文件重名，程序会提示错误，不会执行修改。

### 复制到...

选择目标文件夹，把当前文件或文件夹复制过去。

如果目标位置已经存在同名文件，程序会提示错误。

### 移动到...

选择目标文件夹，把当前文件或文件夹移动过去。

程序会阻止把文件夹移动到它自己或它自己的子目录里。

### 删除

把文件或文件夹移入回收站。删除文件夹时，整个子树一起进入回收站。

## 13. 设置窗口

点击顶部“设置”按钮打开。

### 默认展开深度

设置“默认层级”按钮展开到第几层。可选范围是 1 到 8。

这个设置不会在扫描完成后自动生效，需要点击“默认层级”按钮才会按新深度展开。

### 子节点排序

选择“大小”时，子节点按总大小从大到小排列。

选择“名称”时，子节点按名称排列。

### 动效强度

有三个选项：

- 标准
- 减弱
- 关闭

节点多、动画卡的时候，可以改成“减弱”或“关闭”。

### 排除目录

在这里填写目录名称，每行一个。填写后点击“完成”，重新扫描时，同名目录会被标记为已排除，并且不会继续展开内部内容。

例如填写 `node_modules`，以后扫描时就不会进入各个 `node_modules` 文件夹内部。

### 右键菜单

这里控制 Windows 右键菜单里的“用 FileVisualizer 打开”选项。

默认是禁用状态。点击“启用”后，在资源管理器里右键文件或文件夹，会出现“用 FileVisualizer 打开”。

点击“禁用”可以移除这个右键菜单项。

### 扫描缓存

点击“清理缓存”会删除程序保存的扫描缓存。

缓存存放在：

```text
%APPDATA%\com.cxz.filevisualizer\cache
```

清理缓存后，下次打开同一个目录会重新扫描。

## 14. 常见问题

### 关闭窗口后程序还在运行

这是正常的。关闭窗口只是隐藏到托盘。想退出时，用托盘菜单里的“退出”。

### 全局快捷键没有反应

先确认程序还在托盘运行。如果快捷键和别的软件冲突，可以先用托盘图标或左键单击托盘图标显示窗口。

### 删除的文件去哪了

默认进回收站，可以从回收站还原。

### 为什么有些文件夹显示错误图标

通常是权限不足。程序会跳过这个文件夹，不影响其他节点。

### 搜索不到文件

先确认目录已经扫描完成。搜索会覆盖整个已扫描结果，包括未展开的深层节点，不需要手动展开后再搜。

### 目录太大，画布很卡

可以在设置里降低默认展开深度，或者使用“收起全部”，只展开需要看的几个节点。

### 排除目录没有生效

排除规则要在重新扫描后才会生效。修改设置后点击“刷新”。

### 右键菜单里没有“用 FileVisualizer 打开”

到设置里点击“启用”右键菜单，然后重新启动程序。

## 15. 隐私说明

扫描、大小计算和文件操作都在本机完成。程序不会上传文件路径、文件名或目录内容。

## 16. 快捷键汇总

| 按键 | 作用 |
| --- | --- |
| `Z + X + C` | 显示或隐藏主窗口 |
| `Enter` | 打开当前选中的节点 |
| `F2` | 重命名当前选中的节点 |
| `Delete` | 删除当前选中的节点，移入回收站 |

## 17. 版本信息

当前版本：v1.0.0  
程序文件：`file-visualizer.exe`  
安装包：NSIS 和 MSI 各一个
