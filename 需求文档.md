# 文件结构可视化工具 需求文档

| 项目名称 | 文件结构可视化工具 |
| --- | --- |
| 文档版本 | v0.3 |
| 编写日期 | 2026-08-02 |
| 当前状态 | 草稿（技术选型阶段） |
| 目标形态 | Windows 桌面 EXE |

## 1. 项目概述

### 1.1 项目背景

用户需要一款轻量级 Windows 桌面工具：程序后台常驻，通过全局快捷键随时调出；用户将本地文件夹拖入无限画布后，程序递归扫描并完整展示目录层级；每个文件或文件夹卡片显示自身大小，文件夹卡片显示自身及所有子目录内文件的总大小。

### 1.2 产品目标

- 交付可双击安装、运行的 Windows EXE 程序。
- 支持后台常驻、系统托盘、全局快捷键唤起与隐藏。
- 支持拖入文件夹后递归可视化完整目录结构。
- 支持超深、超多节点的目录，不因层级过深而崩溃或明显卡顿。
- 支持在画布内直接对节点执行打开、重命名、移动、删除等文件系统操作。
- 画布界面美观、大方、简洁，节点操作具备平滑过渡动画。
- 保持轻量化，低内存占用，不依赖用户额外安装大型运行时。
- 所有计算和扫描均在本地完成，不上传任何文件信息。

### 1.3 目标用户

- 需要快速理解目录结构、定位大文件或排查磁盘占用的开发者、设计师、资料整理者等。

### 1.4 关键名词

| 名词 | 说明 |
| --- | --- |
| 节点 | 一个文件或一个文件夹 |
| totalSize | 文件节点为自身字节数；文件夹节点为自身及所有后代文件字节数之和 |
| 懒加载 | 展开某节点时才扫描并加载其直接子层 |
| 无限画布 | 支持无限平移与缩放的可视化画布 |

## 2. 功能需求

### 2.1 核心使用流程

1. 用户启动程序，程序默认常驻系统托盘。
2. 用户按全局快捷键调出主窗口。
3. 用户将本地文件夹拖入画布，或通过文件夹选择器输入目录。
4. 程序在后台线程扫描目录，界面实时显示扫描进度。
5. 扫描完成后，目录层级以树形结构平铺在无限画布中。
6. 用户可缩放、平移、展开或收起节点，并查看各节点大小。
7. 用户可对节点执行打开、重命名、移动、删除等操作，画布实时同步结果。

### 2.2 目录输入

- 支持将文件夹拖拽到主窗口画布。
- 支持将文件夹拖拽到托盘图标以唤起并载入。
- 支持通过“选择文件夹”对话框兜底。
- 支持拖入单个文件，以单文件节点展示。
- 支持一次拖入多个文件夹，多个文件夹以多根节点并列展示。

### 2.3 数据模型

- 内存中采用扁平化存储：`Map<nodeId, Node>`，避免深目录嵌套对象导致遍历困难。
- 节点字段：

| 字段 | 说明 |
| --- | --- |
| id | 节点唯一标识 |
| parentId | 父节点标识，根节点为空 |
| name | 文件或文件夹名称 |
| path | 完整路径 |
| type | dir 或 file |
| size | 文件自身字节数，文件夹为 0 |
| totalSize | 自身及所有后代文件总大小 |
| childrenIds | 子节点标识列表 |
| depth | 节点深度 |
| status | loaded / loading / error / filtered |
| extra | 修改时间、扩展名、是否符号链接等扩展信息 |

- 目录结构完整展开，展示所有已扫描节点；超大目录采用懒加载，避免一次性加载百万级节点。

### 2.4 无限画布

- v1 采用树状展开单一视图，不做环形或放射状视图。
- 支持无限平移与缩放，缩放范围建议 10% 至 300%。
- 提供适应画布、定位到指定节点、minimap 等辅助功能。
- 深树采用树形布局（自上而下或自左而右），节点量极大时使用视口裁剪与节点复用。
- 布局算法采用迭代式实现，避免深目录导致递归爆栈。

### 2.5 节点卡片与连线样式

- 文件夹卡片显示：图标、名称、自身及子项累计大小（totalSize）、直接子项数量。
- 文件卡片显示：图标、名称、自身大小、扩展名、修改时间。
- 卡片同时显示大小占比（如“2.34GB · 36.2%”），占比基准默认是拖入的根目录，可在设置中切换为相对父目录。
- 根节点占比固定显示 100%。
- 基准 totalSize 为 0 时，占比显示 0% 或“-”，不允许除零。
- 文件夹卡片提供展开/收起按钮。
- 子节点默认按 totalSize 降序排列，便于定位大文件。
- 提供“收起全部、展开一级、展开到指定层、展开全部（需确认）”等批量操作。
- 父节点到子节点的连线粗细按子节点 totalSize 占父节点 totalSize 的比例分为四级：
  - 比例 > 0.75：第一级（最粗）
  - 0.5 < 比例 ≤ 0.75：第二级
  - 0.25 < 比例 ≤ 0.5：第三级
  - 比例 ≤ 0.25：第四级（最细）
- 父节点 totalSize 为 0 时，其子节点连线使用第四级（最细）。

### 2.6 大小计算规则

- 文件节点：`totalSize = 自身字节数`。
- 文件夹节点：`totalSize = 所有直接子节点 totalSize 之和`。
- 扫描完成后自底向上汇总，扫描过程中可实时累加已完成的子树。
- 大小展示格式化为 B、KB、MB、GB，并保留适当小数位。
- 大小占比 = 节点 totalSize / 基准节点 totalSize，基准默认是根目录，可切换为父目录；基准 totalSize 为 0 时按 0% 处理，展示 0% 或“-”。

### 2.7 搜索与定位

- 支持按名称搜索当前已加载节点。
- 搜索结果高亮，并支持跳转、定位到画布中心。
- 支持查看节点完整路径并一键复制。

### 2.8 后台运行与全局快捷键

- 程序启动后进入托盘，关闭按钮默认隐藏到托盘（可配置为直接退出）。
- 默认全局快捷键建议为 `Win + Shift + F`，可在设置中修改。
- 快捷键注册失败时给出明确提示，并允许用户重新配置。
- 单实例运行：重复启动时唤起已有实例，不重复创建窗口。
- 快捷键唤起时窗口置顶并聚焦；再次按下则隐藏到托盘。

### 2.9 扫描缓存

- 按目录路径缓存扫描结果，包括目录结构与 totalSize。
- 提供缓存有效期、手动刷新、一键清理等机制。
- 缓存文件存放于用户目录，不跟随程序安装目录。
- 目录内容变化后，重新扫描以刷新缓存。

### 2.10 错误与边界处理

- 无权限目录标记为“读取失败”，不中断整体扫描。
- 符号链接记录真实路径，防止循环引用导致无限递归。
- 对 node_modules 等巨型目录提供排除规则或扫描前提示。
- 支持扫描中途取消、暂停、重新扫描。

### 2.11 设置项

- 全局快捷键。
- 启动后是否自动隐藏到托盘。
- 关闭按钮行为。
- 子节点排序方式。
- 默认展开深度。
- 缓存管理与清理。
- 排除目录规则。
- 文件操作确认策略（删除、移动、覆盖是否每次确认）。
- 大小占比基准（默认根目录，可切换为相对父目录）。
- 动效强度（标准 / 减弱 / 关闭）。

### 2.12 文件系统操作

- 所有文件系统写操作由 Rust 后端执行，前端只发起请求与展示结果，不直接调用浏览器文件 API。

打开与定位：

- 双击文件或文件夹卡片，调用系统默认程序打开。
- 提供“打开所在位置”，在资源管理器中定位并选中目标。

重命名：

- 支持 F2 或右键菜单触发，卡片内联编辑名称。
- 校验非法字符、同名冲突、只读属性与文件占用。
- 成功后更新节点名称与路径，并刷新受影响祖先节点的展示状态。

移动：

- 支持在画布内将节点拖拽到目标文件夹卡片。
- 支持右键菜单“移动到”并选择目标目录。
- 同盘移动使用文件系统重命名语义；跨盘自动降级为复制加删除并显示进度。
- 禁止把文件夹移动到自身或自身子目录内，执行前校验并提示。

删除：

- 默认进入系统回收站，使用 Windows Shell API 实现，不直接永久删除。
- 提供“永久删除”作为高级选项，仅限单个明确目标且需二次确认。
- 删除文件夹视为整棵子树操作，前端同步移除该子树。

复制与粘贴：

- 支持 Ctrl+C / Ctrl+V 复制文件或文件夹到目标目录，作为可选项进入 v1，优先级低于打开、重命名、移动、删除。
- 同名冲突时询问“覆盖 / 保留两者 / 取消”。

交互入口：

- 节点支持多选，提供右键上下文菜单：打开、打开所在位置、重命名、复制、剪切、粘贴、移动到、删除。
- 快捷键：Enter 打开、F2 重命名、Delete 删除、Ctrl+C / Ctrl+X / Ctrl+V。
- 移动、删除、覆盖等危险操作执行前弹出确认。

数据联动：

- 文件系统操作成功后，由 Rust 返回操作结果，前端同步更新节点树。
- 移动或删除后，对受影响祖先节点重新计算 totalSize 并刷新大小展示。
- 被操作目录的扫描缓存标记为失效，下次打开自动刷新。

错误反馈：

- 文件被占用、无权限、路径过长、目标已存在等错误以可读提示展示，不中断其他操作。
- 符号链接操作只作用于链接本身，不递归删除或移动链接目标。

### 2.13 界面与动效

- 整体视觉要求美观、大方、简洁：配色克制、留白合理，卡片、连线与背景对比清晰，不使用过度装饰。
- 布局合理：树状节点排列稳定、不重叠，卡片尺寸统一，名称与大小文字不溢出；窗口缩放和画布缩放时布局保持整齐。
- 节点操作具备对应动画：
  - 展开/收起子树时，节点出现、位移与连线伸缩平滑过渡。
  - 拖拽节点时节点跟随指针，悬停目标文件夹时高亮落点并显示移动预览。
  - 选中与悬停节点时提供轻量高亮反馈。
  - 重命名、移动、删除等文件操作成功后，节点与大小展示带过渡动画。
  - 搜索定位与适应画布时平滑滚动、缩放。
  - 扫描与加载过程提供进度反馈动画。
- 动画时长建议 150-300ms；批量展开/收起或超大节点量时自动降级或关闭动画，优先保证流畅度。
- 设置中提供“减弱动效”与“关闭动效”选项。

### 2.14 范围外（暂不实现）

- v1 不做批量永久删除，永久删除仅限单个明确目标且需二次确认。
- v1 不提供操作撤销/重做（删除进回收站后可由用户手动还原）。
- v1 不做云端同步与多端数据共享。
- v1 不做远程目录、网络驱动器、云盘挂载目录的专项优化。

## 3. 非功能需求

### 3.1 性能

- 十万级节点目录可流畅缩放与平移，目标交互帧率不低于 30fps。
- 打开已缓存目录的首屏渲染时间目标不超过 2 秒。
- 扫描过程不阻塞界面，进度实时反馈。

### 3.2 资源占用

- 常驻内存目标低于 150MB（含 WebView2 场景），隐藏状态尽量只保留后台进程。
- 安装包体积目标不超过 30MB。
- 扫描完成后释放不需要的临时数据。

### 3.3 兼容性

- 支持 Windows 10 / Windows 11 x64。
- 依赖 WebView2 运行时，系统缺失时安装包自动引导安装或内置引导。

### 3.4 稳定性

- 扫描过程中任意单目录失败不影响整体运行。
- 深目录、超长路径不导致崩溃或栈溢出。
- 取消扫描、切换目录、重复拖入时程序保持可用。
- 文件操作失败（占用、无权限、冲突）不影响画布其他功能，界面状态保持一致。

### 3.5 隐私与安全

- 全部扫描与计算在本地完成。
- 程序不上传、不统计任何文件路径或文件名。
- 文件系统操作仅在用户主动发起时执行。
- 删除操作默认进入回收站；除用户二次确认外，不执行永久删除。

## 4. 技术选型

### 4.1 推荐方案

- 框架：Tauri 2。
- 后端：Rust，负责目录扫描、大小汇总、缓存、文件系统操作、全局快捷键、托盘、单实例。
- 前端：React + TypeScript。
- 画布：React Flow，提供无限画布、缩放平移、节点虚拟化、自定义卡片。
- 布局：参考 D3 树形布局算法，需改为迭代式实现以支持深树。
- 状态管理：Zustand。
- 通信：Tauri Command / Event，扫描结果分块推送到前端。

### 4.2 选择理由

- 最终产物为小体积 EXE，满足轻量化要求。
- 托盘、全局快捷键、单实例均有官方或成熟插件支持。
- WebView 使用系统 WebView2，常驻内存远低于 Electron。
- React Flow 生态成熟，无限画布与节点卡片交互开发效率高。

### 4.3 备选方案

| 方案 | 优势 | 劣势 |
| --- | --- | --- |
| Electron | 生态最成熟 | 打包体积大、常驻内存高 |
| WPF / WinUI | 原生、资源占用更低 | 画布与布局需大量自研，开发成本高 |
| Rust + egui | 二进制最小、内存最低 | 画布交互与节点渲染开发量最大 |

### 4.4 明确排除

- 纯浏览器 Web 方案：受浏览器权限与性能限制，无法满足后台常驻、全盘快速扫描与 EXE 交付要求。

## 5. 模块划分

- Rust 后端模块：
  - 目录扫描器：递归遍历、错误跳过、符号链接防循环。
  - 大小汇总器：自底向上计算 totalSize。
  - 缓存模块：扫描结果持久化与失效管理。
  - 文件操作模块：打开、重命名、移动、删除（回收站）、复制与冲突处理。
  - 系统集成：托盘、全局快捷键、单实例、窗口显示与隐藏。
- 前端模块：
  - 画布渲染：节点布局、缩放平移、视口裁剪。
  - 节点卡片：文件/文件夹卡片、展开收起、大小展示。
  - 搜索定位：名称搜索与画布跳转。
  - 操作交互：多选、右键菜单、快捷键、画布内拖拽移动、确认对话框与错误提示。
  - 设置界面：快捷键、缓存、排序、排除规则。
- 通信层：
  - 前端调用 Rust 发起扫描。
  - Rust 通过事件分块推送节点数据与进度。
  - 文件操作请求与结果事件：前端发起，Rust 执行后返回结果，前端刷新节点与大小。

## 6. 验收标准

1. 安装后可通过托盘菜单和全局快捷键唤起、隐藏窗口。
2. 拖入本地文件夹后可递归展示完整层级与节点大小。
3. 十万级节点目录缩放、平移无明显卡顿。
4. 无权限目录、符号链接循环不导致程序崩溃。
5. 二次打开同一目录时缓存生效，明显减少扫描时间。
6. 安装包体积、常驻内存符合非功能需求指标。
7. 程序关闭后仍常驻托盘，不误退。
8. 双击节点可调用系统默认程序打开，右键可打开所在位置。
9. 重命名与移动后节点路径、大小、缓存同步更新，移动父子目录时能阻止循环。
10. 删除默认进入回收站，目录子树一并移除，相关节点大小重新计算。
11. 文件占用、无权限、同名冲突等操作失败均有明确提示，且不影响其他功能。
12. 连线粗细按子节点占父节点比例四级显示，边界值规则明确。
13. 卡片占比支持根目录/父目录基准切换，根节点显示 100%，空目录不除零。
14. 节点展开、收起、拖拽与文件操作均有对应动效，超大节点量下不卡顿。

## 7. 里程碑

### M1：最小闭环

- 建立 Tauri 2 项目骨架。
- 实现拖入文件夹、递归扫描、树形画布展示。
- 实现 totalSize 汇总与格式化展示。
- 实现托盘、全局快捷键、单实例。

### M2：体验完善

- 实现懒加载、批量展开、搜索定位。
- 实现扫描缓存、刷新与清理。
- 实现打开、重命名、移动、删除（回收站）、复制粘贴与大小联动刷新。
- 实现占比显示、占比基准切换与四级连线粗细。
- 完善界面视觉与节点动效。
- 实现错误处理、排除规则与设置界面。
- 补充深目录、超大目录专项测试。

### M3：性能与交付

- 实现视口裁剪与节点复用。
- 十万级节点压测与性能优化。
- 生成 MSI / NSIS 安装包。
- 发布正式版本与使用说明。

## 8. 开放问题

- 多文件夹拖入后采用“多根并列”还是“统一根节点”展示。
- 是否支持网络路径、映射盘符等场景。
- 是否支持导出结构图（PNG、SVG、JSON）。
- 是否支持两个目录结构对比。
- 复制粘贴是否必须进入 v1，还是延后到 v1.1。
